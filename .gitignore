node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.turbo
coverage/
.vite/


# uploads (local dev file storage)
uploads/

#history
.history/

# Claude Code worktree scratch — checkouts live under .claude/worktrees/
# but are independent git working trees, so git on the parent repo would
# otherwise try to commit them as submodule gitlinks on main.
.claude/